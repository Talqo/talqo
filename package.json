{
	"name": "talqo",
	"version": "0.0.1",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"type": "module",
	"scripts": {
		"actions:check": "actionlint && zizmor --offline .",
		"build": "turbo run build",
		"ci": "bun run actions:check && bun run quality && bun run typecheck && bun run test && bun run build",
		"dev": "turbo run dev --filter=!@talqo/docs --ui=tui",
		"docs:dev": "bun run --filter @talqo/docs dev",
		"e2e": "turbo run e2e --output-logs=errors-only",
		"format": "oxfmt --check",
		"format:fix": "oxfmt .",
		"lint": "oxlint --deny-warnings --report-unused-disable-directives .",
		"lint:fix": "oxlint --deny-warnings --report-unused-disable-directives --fix .",
		"prepare": "husky",
		"quality": "turbo run lint format --output-logs=errors-only",
		"quality:fix": "turbo run lint:fix format:fix --output-logs=errors-only",
		"test": "turbo run test --output-logs=errors-only",
		"typecheck": "turbo run typecheck --output-logs=errors-only"
	},
	"devDependencies": {
		"@commitlint/cli": "^21.2.1",
		"@commitlint/config-conventional": "^21.2.0",
		"husky": "^9.1.7",
		"oxfmt": "0.59.0",
		"oxlint": "1.74.0",
		"oxlint-tsgolint": "^0.25.0",
		"turbo": "2.10.5"
	},
	"packageManager": "bun@1.3.14"
}
